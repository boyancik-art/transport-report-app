(()=>{
const R='tc_retail_routes_v1',T='tc_retail_tickets_v1',D='tc_retail_docs_v3',J='tc_retail_ttn_journal_v1';
const get=(k,d=[])=>{try{return JSON.parse(localStorage.getItem(k)||JSON.stringify(d))}catch{return d}},set=(k,v)=>localStorage.setItem(k,JSON.stringify(v)),n=v=>Number(v)||0,r=v=>Math.round(n(v)*1000)/1000,esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const linesOf=d=>d.lines||d.items||d.products||d.positions||[],money=v=>n(v).toLocaleString('uk-UA',{minimumFractionDigits:2,maximumFractionDigits:2}),kg=v=>n(v).toLocaleString('uk-UA',{maximumFractionDigits:3});
function data(no){const route=get(R).find(x=>x.number===no),tickets=get(T),docs=get(D);if(!route)return null;const points=(route.ticketIds||[]).map(id=>tickets.find(t=>t.number===id)).filter(Boolean).map(t=>{const ds=(t.documentIds||[]).map(id=>docs.find(d=>String(d.id)===String(id))).filter(Boolean),mat=ds.reduce((s,d)=>s+n(d.calc),0),fact=t.actualPallets!==''&&t.actualPallets!=null?n(t.actualPallets):mat,weight=ds.reduce((s,d)=>s+(n(d.sourceWeight)||n(d.calcWeight)||n(d.weight)),0),sum=ds.reduce((s,d)=>s+n(d.sum||d.sourceSum||d.amount),0),lines=[];ds.forEach(d=>linesOf(d).forEach(x=>lines.push({...x,_doc:d.number||d.docNo||d.id})));return{ticket:t,docs:ds,store:t.store||ds[0]?.store||'Магазин',address:t.storeAddress||ds[0]?.storeAddress||'',fact:r(fact),weight:r(weight),sum:r(sum),lines}});return{route,points}}
function nextNum(journal){let seq=Math.max(0,...journal.map(x=>Number(String(x.number||'').replace(/\D/g,''))||0));return()=>`TS_TTN_${String(++seq).padStart(6,'0')}`}
function page(title,body,cls=''){return`<section class="sheet ${cls}"><div class="sheet-inner"><div class="page-title">${title}</div>${body}</div></section>`}
function register(route,points,nums){const tp=r(points.reduce((s,p)=>s+p.fact,0)),tw=r(points.reduce((s,p)=>s+p.weight,0));return page(`РЕЄСТР ЗАВАНТАЖЕННЯ · ${esc(route.number)}`,`<div class="meta"><b>Тип:</b> ${esc(route.type||'Пряме поповнення')} &nbsp; <b>Склад відправки:</b> ${esc(route.warehouse)} &nbsp; <b>Дата:</b> ${esc(route.date)} &nbsp; <b>Водій:</b> ${esc(route.driver)} &nbsp; <b>Авто:</b> ${esc(route.vehicle)}</div><table><thead><tr><th>№</th><th>ТТ / магазин</th><th>Адреса</th><th>ТТН</th><th>Док.</th><th>Факт палет</th><th>Вага, кг</th><th>Пломба</th><th>Підпис</th></tr></thead><tbody>${points.map((p,i)=>`<tr><td>${i+1}</td><td>${esc(p.store)}</td><td>${esc(p.address)}</td><td>${esc(nums[p.ticket.number]||nums._all)}</td><td>${p.docs.length}</td><td>${p.fact}</td><td>${kg(p.weight)}</td><td></td><td></td></tr>`).join('')}</tbody><tfoot><tr><td colspan="5"><b>РАЗОМ</b></td><td><b>${tp}</b></td><td><b>${kg(tw)}</b></td><td colspan="2"></td></tr></tfoot></table>`,'register')}
function ttn(route,p,no,copy){const docs=p.docs.map(d=>d.number||d.docNo||d.id).filter(Boolean).join(', '),rows=p.lines.length?p.lines:p.docs.map(d=>({name:d.name||d.number||'Товар згідно документу',qty:d.qty||'',price:d.price||'',sum:d.sum||d.sourceSum||'',_doc:d.number||d.docNo||d.id})),chunks=[];for(let i=0;i<Math.max(1,rows.length);i+=12)chunks.push(rows.slice(i,i+12));return chunks.map((ch,ci)=>page(`ТОВАРНО-ТРАНСПОРТНА НАКЛАДНА № ${esc(no)}`,`<div class="ttn-head"><div><b>Дата:</b> ${esc(route.date)} &nbsp; <b>Примірник:</b> ${copy}/3</div><div><b>Автомобіль:</b> ${esc(route.vehicle)} ${route.trailer?`&nbsp; <b>Причіп:</b> ${esc(route.trailer)}`:''}</div><div><b>Автомобільний перевізник:</b> ${esc(route.carrier)}</div><div><b>Водій:</b> ${esc(route.driver)}</div><div><b>Вантажовідправник:</b> ТОВ «ТС ПЛЮС» &nbsp; <b>Місце завантаження:</b> ${esc(route.warehouse)}</div><div><b>Вантажоодержувач:</b> ${esc(p.store)} &nbsp; <b>Адреса отримання:</b> ${esc(p.address)}</div><div><b>Супровідні документи:</b> ${esc(docs)}</div></div><div class="cargo-title">ВІДОМОСТІ ПРО ВАНТАЖ ${chunks.length>1?`· стор. ${ci+1}/${chunks.length}`:''}</div><table class="cargo"><thead><tr><th>№</th><th>Найменування вантажу</th><th>Документ</th><th>Кількість</th><th>Ціна без ПДВ</th><th>Сума</th><th>Здав</th><th>Прийняв</th></tr></thead><tbody>${ch.map((x,i)=>`<tr><td>${ci*12+i+1}</td><td>${esc(x.name||x.nomenclature||x.product||x.title||'')}</td><td>${esc(x._doc||'')}</td><td>${esc(x.qty??x.quantity??'')}</td><td>${x.price!=null?money(x.price):''}</td><td>${x.sum!=null?money(x.sum):''}</td><td></td><td></td></tr>`).join('')}</tbody></table><div class="totals"><b>Усього відпущено:</b> ${p.fact} пал. &nbsp; <b>Маса брутто:</b> ${kg(p.weight+p.fact*20)} кг &nbsp; <b>Вага товару:</b> ${kg(p.weight)} кг &nbsp; <b>Сума:</b> ${money(p.sum)} грн</div><div class="sign"><span>Відпуск дозволив __________________</span><span>Здав __________________</span><span>Прийняв __________________</span></div>`,ci?'cargo-cont':'ttn')).join('')}
function consolidated(route,points){const docs=points.flatMap(p=>p.docs),lines=points.flatMap(p=>p.lines),fact=r(points.reduce((s,p)=>s+p.fact,0)),weight=r(points.reduce((s,p)=>s+p.weight,0)),sum=r(points.reduce((s,p)=>s+p.sum,0));return{ticket:{number:route.number},docs,lines,store:route.destinationWarehouse,address:route.destinationWarehouse,fact,weight,sum}}
function html(route,points,nums){const inter=route.deliveryType==='interwarehouse',body=inter?[1,2,3].map(c=>ttn(route,consolidated(route,points),nums._all,c)).join(''):points.map(p=>[1,2,3].map(c=>ttn(route,p,nums[p.ticket.number],c)).join('')).join('');return`<!doctype html><html lang="uk"><head><meta charset="utf-8"><title>ТТН_${esc(route.number)}_${esc(route.date)}</title><style>@page{size:A4 landscape;margin:8mm}*{box-sizing:border-box}body{margin:0;font-family:Arial,sans-serif;color:#111}.sheet{page-break-after:always;width:100%;min-height:190mm}.sheet:last-child{page-break-after:auto}.page-title{text-align:center;font-weight:800;font-size:15px;margin-bottom:5mm}.meta,.ttn-head{font-size:10px;line-height:1.55;margin-bottom:4mm}.ttn-head div{border-bottom:1px solid #aaa;padding:1mm 0}table{width:100%;border-collapse:collapse;font-size:9px}th,td{border:1px solid #222;padding:2mm 1.5mm;vertical-align:top}th{background:#eee}.cargo-title{text-align:center;font-size:11px;font-weight:800;margin:3mm}.cargo td{height:7mm}.totals{font-size:10px;border:1px solid #222;border-top:0;padding:2.5mm}.sign{display:flex;justify-content:space-between;gap:8mm;font-size:9px;margin-top:7mm}.sign span{flex:1}@media screen{body{background:#ddd}.sheet{width:297mm;min-height:210mm;margin:8mm auto;background:#fff;padding:8mm;box-shadow:0 2mm 8mm #0003}}</style></head><body>${register(route,points,nums)}${body}<script>window.onload=()=>setTimeout(()=>window.print(),250)<\/script></body></html>`}
function generate(no){const q=data(no);if(!q||!q.points.length)return alert('У маршруті немає вантажу для формування ТТН.');const {route,points}=q;if(!route.warehouse||!route.carrier||!route.driver||!route.vehicle)return alert('Заповніть склад, перевізника, водія та авто.');const inter=route.deliveryType==='interwarehouse';if(inter&&!route.destinationWarehouse)return alert('Не вказано склад отримувач.');let journal=get(J),next=nextNum(journal),nums={};if(inter){const old=journal.find(x=>x.type==='Міжскладська'&&x.route===route.number);nums._all=old?.number||next()}else points.forEach(p=>{const old=journal.find(x=>x.type==='Магазин'&&x.route===route.number&&x.ticket===p.ticket.number);nums[p.ticket.number]=old?.number||next()});const blob=new Blob([html(route,points,nums)],{type:'text/html;charset=utf-8'}),url=URL.createObjectURL(blob),w=window.open(url,'_blank');if(!w){URL.revokeObjectURL(url);return alert('Дозвольте спливаючі вікна та повторіть.')}setTimeout(()=>URL.revokeObjectURL(url),60000);if(inter){const p=consolidated(route,points),old=journal.find(x=>x.type==='Міжскладська'&&x.route===route.number),rec={number:nums._all,type:'Міжскладська',route:route.number,date:route.date,from:route.warehouse,to:route.destinationWarehouse,carrier:route.carrier,driver:route.driver,vehicle:route.vehicle,trailer:route.trailer||'',documents:p.docs.length,docKeys:p.docs.map(d=>d.key||String(d.id)),pallets:p.fact,weight:p.weight,sum:p.sum,status:'PDF сформовано',createdAt:old?.createdAt||new Date().toISOString()};journal=old?journal.map(x=>x===old?rec:x):[rec,...journal]}else points.forEach(p=>{const old=journal.find(x=>x.type==='Магазин'&&x.route===route.number&&x.ticket===p.ticket.number),rec={number:nums[p.ticket.number],type:'Магазин',route:route.number,ticket:p.ticket.number,date:route.date,from:route.warehouse,to:p.store,address:p.address,carrier:route.carrier,driver:route.driver,vehicle:route.vehicle,documents:p.docs.length,pallets:p.fact,weight:p.weight,sum:p.sum,status:'PDF сформовано',createdAt:old?.createdAt||new Date().toISOString()};journal=old?journal.map(x=>x===old?rec:x):[rec,...journal]});set(J,journal);set(R,get(R).map(x=>x.number===no?{...x,status:'ТТН сформовано',ttnCount:inter?1:points.length}:x));window.RetailRoutes?.render?.()}
window.addEventListener('retail:generate-route-ttn',e=>generate(e.detail?.route));window.RetailRouteTTN={generate};
})();