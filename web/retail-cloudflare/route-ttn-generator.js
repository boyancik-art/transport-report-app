(()=>{
const R='tc_retail_routes_v1',T='tc_retail_tickets_v1',D='tc_retail_docs_v3',J='tc_retail_ttn_journal_v1';
const get=(k,d=[])=>{try{return JSON.parse(localStorage.getItem(k)||JSON.stringify(d))}catch{return d}},set=(k,v)=>localStorage.setItem(k,JSON.stringify(v)),n=v=>Number(v)||0,r=v=>Math.round(n(v)*1000)/1000,esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function linesOf(d){return d.lines||d.items||d.products||d.positions||[]}
function routeData(no){const route=get(R).find(x=>x.number===no),tickets=get(T),docs=get(D);if(!route)return null;const points=(route.ticketIds||[]).map(id=>tickets.find(t=>t.number===id)).filter(Boolean).map(t=>{const ds=(t.documentIds||[]).map(id=>docs.find(d=>String(d.id)===String(id))).filter(Boolean),mat=ds.reduce((s,d)=>s+n(d.calc),0),fact=t.actualPallets!==''&&t.actualPallets!=null?n(t.actualPallets):mat,weight=ds.reduce((s,d)=>s+(n(d.sourceWeight)||n(d.calcWeight)||n(d.weight)),0),sum=ds.reduce((s,d)=>s+n(d.sum||d.sourceSum||d.amount),0),lines=[];ds.forEach(d=>linesOf(d).forEach(x=>lines.push({...x,_doc:d.number||d.docNo||d.id})));return{ticket:t,docs:ds,store:t.store||ds[0]?.store||'Магазин',address:t.storeAddress||ds[0]?.storeAddress||'',fact:r(fact),weight:r(weight),sum:r(sum),lines}});return{route,points}}
const money=v=>n(v).toLocaleString('uk-UA',{minimumFractionDigits:2,maximumFractionDigits:2}),kg=v=>n(v).toLocaleString('uk-UA',{maximumFractionDigits:3});
function ttnNumbers(points,journal,route){let seq=Math.max(0,...journal.map(x=>Number(String(x.number||'').replace(/\D/g,''))||0));const out={};points.forEach(p=>{const old=journal.find(x=>x.type==='Магазин'&&x.route===route.number&&x.ticket===p.ticket.number);out[p.ticket.number]=old?.number||'TS_TTN_'+String(++seq).padStart(6,'0')});return out}
function page(title,body,cls=''){return`<section class="sheet ${cls}"><div class="sheet-inner"><div class="page-title">${title}</div>${body}</div></section>`}
function register(route,points,nums){const totalP=r(points.reduce((s,p)=>s+p.fact,0)),totalW=r(points.reduce((s,p)=>s+p.weight,0));return page(`РЕЄСТР ЗАВАНТАЖЕННЯ · ${esc(route.number)}`,`<div class="meta"><b>Склад відправки:</b> ${esc(route.warehouse)} <b>Дата:</b> ${esc(route.date)} <b>Водій:</b> ${esc(route.driver)} <b>Авто:</b> ${esc(route.vehicle)}</div><table><thead><tr><th>№</th><th>ТТ / магазин</th><th>Адреса</th><th>ТТН</th><th>Док.</th><th>Факт палет</th><th>Вага, кг</th><th>Пломба</th><th>Підпис</th></tr></thead><tbody>${points.map((p,i)=>`<tr><td>${i+1}</td><td>${esc(p.store)}</td><td>${esc(p.address)}</td><td>${esc(nums[p.ticket.number])}</td><td>${p.docs.length}</td><td>${p.fact}</td><td>${kg(p.weight)}</td><td></td><td></td></tr>`).join('')}</tbody><tfoot><tr><td colspan="5"><b>РАЗОМ</b></td><td><b>${totalP}</b></td><td><b>${kg(totalW)}</b></td><td colspan="2"></td></tr></tfoot></table><div class="sign"><span>Час пломбування __________________</span><span>Водій __________________</span><span>Відповідальний __________________</span></div>`,'register')}
function cargoRows(p){const rows=p.lines.length?p.lines:p.docs.map(d=>({name:d.name||d.number||'Товар згідно документу',qty:d.qty||'',price:d.price||'',sum:d.sum||d.sourceSum||'',_doc:d.number||d.docNo||d.id}));return rows}
function ttnCopy(route,p,no,copy){const docs=p.docs.map(d=>d.number||d.docNo||d.id).filter(Boolean).join(', '),rows=cargoRows(p),chunks=[];for(let i=0;i<Math.max(1,rows.length);i+=12)chunks.push(rows.slice(i,i+12));return chunks.map((chunk,ci)=>page(`ТОВАРНО-ТРАНСПОРТНА НАКЛАДНА № ${esc(no)}`,`<div class="ttn-head"><div><b>Дата:</b> ${esc(route.date)} &nbsp; <b>Примірник:</b> ${copy}/3</div><div><b>Автомобіль:</b> ${esc(route.vehicle)} ${route.trailer?`&nbsp; <b>Причіп:</b> ${esc(route.trailer)}`:''}</div><div><b>Автомобільний перевізник:</b> ${esc(route.carrier)}</div><div><b>Водій:</b> ${esc(route.driver)}</div><div><b>Вантажовідправник:</b> ТОВ «ТС ПЛЮС» &nbsp; <b>Місце завантаження:</b> ${esc(route.warehouse)}</div><div><b>Вантажоодержувач:</b> ${esc(p.store)} &nbsp; <b>Адреса:</b> ${esc(p.address)}</div><div><b>Супровідні документи:</b> ${esc(docs)}</div></div><div class="cargo-title">ВІДОМОСТІ ПРО ВАНТАЖ ${chunks.length>1?`· стор. ${ci+1}/${chunks.length}`:''}</div><table class="cargo"><thead><tr><th>№</th><th>Найменування вантажу</th><th>Документ</th><th>Кількість</th><th>Ціна без ПДВ</th><th>Сума</th><th>Здав</th><th>Прийняв</th></tr></thead><tbody>${chunk.map((x,i)=>`<tr><td>${ci*12+i+1}</td><td>${esc(x.name||x.nomenclature||x.product||x.title||'')}</td><td>${esc(x._doc||'')}</td><td>${esc(x.qty??x.quantity??'')}</td><td>${x.price!=null?money(x.price):''}</td><td>${x.sum!=null?money(x.sum):''}</td><td></td><td></td></tr>`).join('')}${Array.from({length:Math.max(0,12-chunk.length)},()=>'<tr class="blank"><td>&nbsp;</td><td></td><td></td><td></td><td></td><td></td><td></td><td></td></tr>').join('')}</tbody></table><div class="totals"><b>Усього відпущено:</b> ${p.fact} пал. &nbsp; <b>Маса брутто:</b> ${kg(p.weight+p.fact*20)} кг &nbsp; <b>Вага товару:</b> ${kg(p.weight)} кг ${p.sum?`&nbsp; <b>Сума:</b> ${money(p.sum)} грн`:''}</div><div class="sign"><span>Відпуск дозволив __________________</span><span>Здав __________________</span><span>Прийняв __________________</span></div>`,ci?'cargo-cont':'ttn')).join('')}
function packetHTML(route,points,nums){return`<!doctype html><html lang="uk"><head><meta charset="utf-8"><title>ТТН_${esc(route.number)}_${esc(route.date)}</title><style>@page{size:A4 landscape;margin:8mm}*{box-sizing:border-box}body{margin:0;font-family:Arial,sans-serif;color:#111;background:#fff}.sheet{page-break-after:always;width:100%;min-height:190mm}.sheet:last-child{page-break-after:auto}.sheet-inner{padding:2mm}.page-title{text-align:center;font-weight:800;font-size:15px;margin:0 0 5mm}.meta,.ttn-head{font-size:10px;line-height:1.55;margin-bottom:4mm}.ttn-head div{border-bottom:1px solid #aaa;padding:1mm 0}table{width:100%;border-collapse:collapse;font-size:9px}th,td{border:1px solid #222;padding:2mm 1.5mm;vertical-align:top}th{background:#eee;text-align:center}.cargo-title{text-align:center;font-size:11px;font-weight:800;margin:3mm 0 2mm}.cargo td{height:7mm}.cargo .blank td{height:7mm}.totals{font-size:10px;border:1px solid #222;border-top:0;padding:2.5mm}.sign{display:flex;justify-content:space-between;gap:8mm;font-size:9px;margin-top:7mm}.sign span{flex:1}.register table td{height:8mm}@media screen{body{background:#ddd}.sheet{width:297mm;min-height:210mm;margin:8mm auto;background:#fff;box-shadow:0 2mm 8mm #0003;padding:8mm}}@media print{.sheet{min-height:auto}}</style></head><body>${register(route,points,nums)}${points.map(p=>[1,2,3].map(c=>ttnCopy(route,p,nums[p.ticket.number],c)).join('')).join('')}<script>window.onload=()=>setTimeout(()=>window.print(),250)<\/script></body></html>`}
function downloadPacket(route,points,nums){const html=packetHTML(route,points,nums),blob=new Blob([html],{type:'text/html;charset=utf-8'}),url=URL.createObjectURL(blob),w=window.open(url,'_blank');if(!w){URL.revokeObjectURL(url);alert('Браузер заблокував вікно друку. Дозвольте спливаючі вікна для цього сайту та повторіть.');return false}setTimeout(()=>URL.revokeObjectURL(url),60000);return true}
function generate(no){const q=routeData(no);if(!q||!q.points.length)return alert('У маршруті немає магазинів для формування ТТН.');const {route,points}=q;if(!route.warehouse)return alert('Вкажіть склад відправлення.');if(!route.carrier||!route.driver||!route.vehicle)return alert('Заповніть перевізника, водія та авто перед формуванням ТТН.');let journal=get(J),created=[],nums=ttnNumbers(points,journal,route);if(!downloadPacket(route,points,nums))return;points.forEach(p=>{const old=journal.find(x=>x.type==='Магазин'&&x.route===route.number&&x.ticket===p.ticket.number),number=nums[p.ticket.number],rec={number,type:'Магазин',route:route.number,ticket:p.ticket.number,date:route.date,from:route.warehouse,to:p.store,address:p.address,carrier:route.carrier,driver:route.driver,vehicle:route.vehicle,trailer:route.trailer||'',documents:p.docs.length,docKeys:p.docs.map(d=>d.key||String(d.id)),pallets:p.fact,weight:p.weight,status:'PDF сформовано',createdAt:old?.createdAt||new Date().toISOString()};if(old)journal=journal.map(x=>x===old?rec:x);else created.push(rec)});set(J,[...created,...journal]);set(R,get(R).map(x=>x.number===no?{...x,status:'ТТН сформовано',ttnCount:points.length}:x));}
window.addEventListener('retail:generate-route-ttn',e=>generate(e.detail?.route));window.RetailRouteTTN={generate};
})();