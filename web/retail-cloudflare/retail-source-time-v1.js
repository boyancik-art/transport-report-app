/* Source document time policy: never derive business cutoff time from import/createdAt timestamps. */
(()=>{'use strict';const S=window.RetailState;if(!S)return;
const timeOf=d=>{for(const v of [d.documentTime,d.sourceDocumentTime,d.orderTime,d.time]){const s=String(v||'').trim();const m=s.match(/(?:^|T|\s)([01]\d|2[0-3]):([0-5]\d)(?::[0-5]\d)?(?:$|\s|Z|[+-])/);if(m)return`${m[1]}:${m[2]}`;if(/^([01]\d|2[0-3]):[0-5]\d$/.test(s))return s}return''};
function enforce(){if(location.hash!=='#base-excel')return;const cut=document.querySelector('[data-filter="cut"]')?.value||'';document.querySelectorAll('input[data-doc]').forEach(cb=>{const k=cb.dataset.doc,d=S.documents().find(x=>S.docKey(x)===k);if(!d)return;const t=timeOf(d),row=cb.closest('tr');if(row){const cell=row.children[3];if(cell)cell.textContent=t||'—'}if(cut&&t&&t>cut){cb.checked=false;cb.disabled=true;row?.classList.add('rt-after-cutoff')}else if(!window.RetailSourceOrdersGuard?.usedKeys?.().has(k)){cb.disabled=false;row?.classList.remove('rt-after-cutoff')}})}
document.addEventListener('change',e=>{if(e.target.matches('[data-filter="cut"]'))setTimeout(enforce,0)});new MutationObserver(()=>setTimeout(enforce,20)).observe(document.querySelector('.content')||document.body,{childList:true,subtree:true});window.addEventListener('hashchange',()=>setTimeout(enforce,30));setTimeout(enforce,30);window.RetailSourceTime={timeOf,enforce};})();